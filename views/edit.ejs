<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./style.css" />
  </head>
  <body class="text-gray-900">
    <%- include("partials/navbar") %>
    <div class="flex justify-center items-center p-10">
      <form
        class="w-[1000px] bg-white p-6 rounded-lg border-2 border-gray-300 mt-10"
        action="/updateShortCode"
        method="post"
      >
        <h1 class="text-2xl font-semibold text-center mb-6">Edit Shortened URL</h1>

        <input
          class="w-full px-4 py-2 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-gray-500"
          type="text"
          name="id"
          hidden
          value="<%= id %>"
          placeholder="https://example.com"
          required
        />
        <label class="block text-sm font-medium mb-1">Enter URL</label>
        <input
          class="w-full px-4 py-2 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-gray-500"
          type="url"
          name="url"
          value="<%= url %>"
          placeholder="https://example.com"
          required
        />

        <label class="block text-sm font-medium mt-4 mb-1">Short Code</label>
        <input
          class="w-full px-4 py-2 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-gray-500"
          type="text"
          name="shortCode"
          value="<%= shortCode %>"
          placeholder="Enter Short Code"
          required
        />

        <button
          type="submit"
          class="mt-5 px-4 py-2 bg-gray-800 text-white rounded-md hover:bg-gray-700 transition"
        >
          Update Short Link
        </button>
      </form>
    </div>
    <script
      type="module"
      src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"
    ></script>
    <script
      nomodule
      src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"
    ></script>
  </body>
</html>
