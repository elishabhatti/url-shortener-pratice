<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./style.css" />
  </head>
  <body class="text-gray-900">

    <div class="flex justify-center items-center p-10">
      <form class="w-[1000px] bg-white p-6 rounded-lg border border-gray-300 mt-10" action="/addShortCode" method="post">
        <h1 class="text-2xl font-semibold text-center mb-6">URL Shortener</h1>

        <label class="block text-sm font-medium mb-1">Enter URL</label>
        <input
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-gray-500"
          type="url"
          name="url"
          placeholder="https://example.com"
          required
        />

        <label class="block text-sm font-medium mt-4 mb-1">Short Code</label>
        <input
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-gray-500"
          type="text"
          name="shortCode"
          placeholder="Enter Short Code"
          required
        />

        <button type="submit" class="w-full mt-5 py-2 bg-gray-800 text-white rounded-md hover:bg-gray-700 transition">
          Create Short Link
        </button>

        <!-- Shortened Links Display -->
        <div class="mt-6">
          <h2 class="text-lg font-medium mb-3">Your Short Links</h2>
          <div class="space-y-2 text-sm">
          </div>
        </div>
      </form>
    </div>
  </body>
</html>
